#!/usr/bin/env bash
set -euo pipefail

if [ -f ".venv/bin/activate" ]; then
  source .venv/bin/activate
fi

# Force local repo data path (works on laptop)
export CHURN_MLOPS_DATA_DIR="$(pwd)/data"
export CHURN_MLOPS_RAW_DIR="$(pwd)/data/raw"

python -m churn_mlops.data.validate
